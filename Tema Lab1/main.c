#include <stdio.h>
#include "Ui/console.h"
#include "Tests/tests.h"
int main(void) {
    runTests();
    // Offer_list* l=createOfferList();
    // run(l);
    // destroyOfferList(l);
    return 0;
}
// good luck, daca ai nevoie de ajutor imi poti scrie pe disco/wp
// daca nu, lasa un review bun bro

